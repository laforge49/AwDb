package org.agilewiki.awdb.nodes;

import org.agilewiki.awdb.NodeBase;

public class Role_Node extends NodeBase {

    public Role_Node(String nodeId, long timestamp) {
        super(nodeId, timestamp);
    }
}
